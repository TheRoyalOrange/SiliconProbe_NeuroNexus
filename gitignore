# === OS clutter ===
.DS_Store
Thumbs.db
desktop.ini

# === MATLAB ===
*.asv          # autosave files
*.m~
slprj/
*.slxc
octave-workspace

# === Data & large files ===
# Nothing is being ignored by extension yet since no large/raw
# data files are checked in currently. Uncomment / add patterns
# below as soon as raw or bulky data starts living inside the
# repo folders (common candidates for a pipeline like this):
# *.mat
# *.h5
# *.avi
# *.mp4
# /data/raw/

# === Editor / environment ===
.vscode/
*.code-workspace

# === Claude Code local settings (personal, not shared) ===
.claude/settings.local.json
